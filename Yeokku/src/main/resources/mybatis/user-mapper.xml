<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myuser">
	


	<select id="idchk" resultType="user_dto">
		SELECT  USER_NO, USER_ID, USER_PW, USER_NAME, USER_EMAIL, USER_NICKNAME, USER_POSTCODE, USER_ADDRESS, USER_EXTRAADDRESS, USER_DETAILADDRESS, USER_REG_DATE, USER_ABLE, USER_ROLE
		FROM USERBOARD
		WHERE USER_ID=#{user_id}
	</select>

	

</mapper>  